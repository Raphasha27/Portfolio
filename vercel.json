{
  "framework": "vite",
  "buildCommand": "cd v2 && npm install && npm run build",
  "outputDirectory": "v2/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}